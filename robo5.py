from robo1 import coordenadas
import pyautogui as p

#ele recebe uma cordenada x e uma cordenada Y movendo o mouse para devidas cordenadas
# p.moveTo(x=44, y=451, duration=1)
# p.click(x=44, y=451)
# p.click(x=44, y=451)

with open(coordenadas.txt) as arquivo:
    for line in arquivo:
        ...
